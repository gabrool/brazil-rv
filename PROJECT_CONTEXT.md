# Brazil-RV project context

Current contract: 2026-09-21. This is the startup contract, not an experiment log.
The previous context is preserved byte-for-byte in [historical context](docs/PROJECT_CONTEXT_HISTORY.md), SHA b80c3539acaed9bfdde84a871b630d9d43fb7e9badcf8b54f0e315da14f76044.
Its older pending queues, permissions and limitations are superseded where resolved below. Read historical sections only when relevant.

## Objective and execution

September 21 user correction: investigate the wider-attention performance reversal
before further capacity fits. The LSTM plans are frozen but no LSTM fit has begun.
Resolve `scaling_investigation` and `scaling_fit_audit`; follow
[the investigation contract](docs/v2_SCALING_INVESTIGATION.md). Existing C/D screens
remain evidence, but their failed gates do not bar this newly authorized diagnostic
and broader chronological evaluation. Eight development evaluation folds are
fixed, with six reserved from new corrected comparisons; none is retroactively
called historically untouched. Keep 2025/2026 consumers unopened. The user also
authorizes deleting stale files; prefer reproducible caches and verified redundant
artifacts, preserving unique research evidence and immutable sources.

Resolve `scaling_parent_patience_plan` for the outcome-informed TE_wide parent
seed29 patience5→20 test, followed by the unchanged F10 child recipe. The first
nine corrected-control epochs and all model weights reproduce exactly. Its F10
seed/ensemble gains7.04780/2.36412bps/day, remaining negative aboveCDI; the original
fits remain selected controls. This diagnostic is not model adoption. Resolve
`scaling_expanded_fit_plan`/`scaling_expanded_evaluation_plan` for the eight-period
unchanged-baseline comparison, with6P/24F/48books reused and24F/48books new.
The completed depth and native GRU96 width waves' redundant intermediate epoch
files now reside only in verified recovery archives; selected checkpoints,
forecasts and books stay online. Resolve `scaling_storage` before requesting them.

The added F3 evaluation exposed Fibria, Guararapes and QGEP held transitions.
`scaling_expanded_source_plan` supplies a separate account-only overlay, with
original dated evidence and twelve qualified books. Pending corporate cash can
now change only on its announced revision date, preserving original entitlement
units after share disposal. All earlier terms have no revisions and retain their
executed branch. The accepted model store and active fits remain unchanged:
Guararapes' May2019 split (8, not inferred7.457812) and QGEP's April2019 dividend
(q1/cash1.90684828735, not inferredq1.0879766/cash0) expose remaining model-data
dependencies. Do not describe the expanded comparison as globally source-corrected.
Reuse its twelve source replays and independent account/entitlement proofs;
resolve `scaling_expanded_source_qualification`/`scaling_expanded_event_qualification`.

Complete the user-authorized [economic/data/capacity registration](research/preregistrations/v2_economic_data_scaling.md). Foundation is complete and sealed; never repeat it. Resolve [the run pointer](docs/v2_economic_data_scaling_run.json) and [progress](docs/v2_economic_data_scaling_progress.md) for evidence.

The user disabled the one-minute heartbeat on September20 and requested continuous implementation without routine checkpoint pauses. The automation is paused; research remains active. Do not re-enable it or create another recurring monitor. Inspect actual process command lines before launches; never duplicate workers. Report meaningful results or blockers.

Use the local RTX2060 only. No Lambda, deployment, forward capture, new subagents or held-out2025/2026 consumer reads. Preserve all933 permanent names,3717 accepted sessions,2009 warmup and full60-session history. Keep original graphs/folds/seeds/optimizer/selector/learning budgets unless a registered contrast changes them. Reread registration after each stage and GPU wave.

Set `TORCHINDUCTOR_CACHE_DIR=C:/quant-data/b3/interim/ti` for GPU compilation. The default Windows temporary cache produced an unwritable264-character Triton artifact name; the same relative filename writes at238characters in this short derived-cache path. This changes cache location only. Preserve existing compiler-failure evidence and completed fits; resolve `stage_c_refit_cache_path_resolution`.

## Admission and canonical inputs

The [compiled forecast failure](docs/v2_COMPILED_SCORE_FAILURE.md) is resolved to
an unsafe fused GRU96 residual LayerNorm reduction. Qualified ATen normalization
and backward preserve model equations within compilation. Resolve
stage_d_compiler_acceptance. Five other C/D architecture controls reproduce all
126 F10/11 forecast dates and pass temporary-memory poisoning; C and TE128 results
are retained. This is bounded scope evidence, not an exhaustive checkpoint proof.
All original GRU96 width results are excluded and preserved. The tested original
P batch passed but contains the same unsafe operation; three fresh parents and
twelve children remove that uncertainty without claiming an observed P mismatch.
stage_d_width_plan now selects capacity_width_recovery: ten TE128 fits are reused,
five remain under original runtime3b0f921, and fifteen GRU96 fits use isolated
normalization@308007a. A declared junction references original TE128 directories
without copies. Twelve primary/132ordinary/12funded TE128 books and84skips are
reused; no source/store/account proof is repeated. All30 accepted fits and their
portfolio scenarios are complete. [Width results](docs/v2_CAPACITY_RESULTS.md)
reject both larger widths: TE128-minus-96 -.70231 and GRU96-minus-64 -.69412
bps/day, each only1/4 positive folds. Resolve stage_d_width_results/admission.
Retain attention96/GRU64; neither larger width is adopted. Width recovery is
verified through stage_d_width_recovery. The depth wave is complete and rejected:
TE_depth2/GRU_depth2 add only a second encoder layer, with1,643,683/1,505,411
parameters. Resolve stage_d_depth_plan/evaluation_plan;30 fresh P/F fits preserve
all original seeds/folds/learning budgets. Attention depth changes net by-.18921
bps/day; GRU depth gains1.11702 but only1/3 seeds and2/4 folds improve. Neither
passes the registered screen; retain one layer for both. Resolve
stage_d_depth_results/admission. Matched LSTM and final conditional branches remain.
Original stage_d_width_original_plan/evaluation and all failed attempts remain.

stage_d_width_forecast_qualification admits exact saved/fresh compiled and
NaN-poisoned reproduction for all51,657 valid values of preselected GRU96 F2/11.
Its original eager-allclose check still fails two MPLU horizons on2018-11-01;
differences .00302124/.00312805 remain explicit, with identical-input eager FP32
references. No threshold/score/weight was changed. This is qualified reproduction
with a stable mixed-precision limitation, not a passed original tolerance test.

The [three-block closeout](docs/v2_STAGE_A_CLOSEOUT.md) is complete for its bounded contract. [Integrated StageA](docs/v2_ECONOMIC_ACCOUNT_ACCEPTANCE.md) is accepted with explicit conditional exposure limits. Registered C is active: [matched replays](docs/v2_MATCHED_ECONOMIC_REPLAYS.md) expose additional sourced corporate transitions and the JSL identifier reuse. There are160 combined old-coordinate account/source books,100 corrected and60 reused; all1,062frozen sensitivities pass saved accounting checks, with618explicit skips. Some recall/denial stress paths retain unresolved obligations; accounting parity is not proof of executable resolution. Complete matched data and all60 compatible refits are accepted. [Matched results](docs/v2_MATCHED_RESULTS.md) dispose of Stage C: R$10m four-fold net-CDI means full attention .99921, wider attention -.14375, GRU .68069 and C6 .45646 bps/day. Both nominated alternatives trail full attention, so their ten-fold replications stop; no model is adopted. Resolve stage_c_refit_results/admission/completion. Stage D's registered attention96→128 and GRU64→96 wave is disposed under the compiler recovery disposition above; all original contrasts and controls remain frozen. No held-out or live profitability/capacity claim follows. CPU audit timings are not fit ETAs.

Reuse V31-V41, historical-cost, source, feature and consumer proofs. New engineering requires a changed implementation, untested interaction or actual exposure. Freeze contrasts before outcomes; do not repeat passed matrices for a checkpoint. The closeout rows are not new experiment campaigns.

Current refit input: [matched data acceptance](docs/v2_matched_data_inputs.json), resolved by economic_refit_inputs/matched_data_inputs. It selects the complete store on C, manifest fd4ad71a0efaad0914520c89d589e0c0dfd9665158204c4721f821f9df8d4161. [Matched evidence](docs/v2_MATCHED_ECONOMIC_REPLAYS.md) and [fit contract](docs/v2_matched_store_contract.json) bind all106arrays/933names/3717dates/full60. Earlier [composed acceptance](docs/v2_composed_data_inputs.json), manifest fab704fccbd7ceed0624bcfcc67178327f392e43d124242caf8d77ff1c64dfc0 on D, remains immutable. New finalized arrays use lossless LZX compression; decoded hashes and independent composition pass.

Preserve [the economic baseline](docs/v2_economic_data_inputs.json), [Natura parent](docs/v2_natura_data_inputs.json), [foundation inputs](docs/v2_data_inputs.json), all original sources and old fit roots. Never overwrite accepted stores. All106 arrays retain the same feature schema; schema equality does not mean compatible coordinates. New training-only conditioning and P weights are required; only compatible new F children inherit. The existing transfer guard rejects changed coordinates before reading old weights.

Accounting/source-only replay shallow-copies frozen OLD PolicyData and replaces explicit account/source inputs only. Reconstructing PolicyData regenerates static coordinates and destroys attribution. Keep accounting, rate/availability, data and refit effects separate. Old fits never read changed coordinates.

Fresh matched forecasts use the separately qualified new portfolio cache (stage_c_refit_economics and stage_c_refit_economics_qualification), with original allocation-risk formulas on admitted dated histories. It is never supplied to old fits. The frozen stage_c_data_replay_plan binds all96 completed seed/ensemble/capital books and original neutral calibration. Ordinary/funded/precision sensitivities complete1065/90/9 books with615/6/39 explicit unexposed skips. Reuse all completed books. The unused July18 2016 loader prelude and July19 prior-CDI feature remain explicitly outside every actual evaluation window; each actual window has corrected current/prior coverage.

The current portfolio-cache binding includes the two-cell cleared-action index correction recorded by stage_c_refit_action_identity. JSL/SIMPAR and TIM conversion distributions remain unchanged; their cleared ordinary-action indices must identify themselves, never use−1. Separate qualified plan files preserve the original plans/cache and completed F2 books. Static PolicyData, model-store coordinates and all fits remain unchanged. Reuse prior cache/risk/source proofs and the four actual account-schema checks; do not rebuild them.

## Information, identity and targets

Decision time is15:45 America/Sao_Paulo. Daily market state ends at t-1; publications enter at their admitted availability; completed intraday bars exclude the entry bar. Use the dated B3 schedule. Source completeness, observation, activity, membership, feature validity and label validity are distinct. Do not invent/interpolate OHLC, mark stale prices observed, or use stale exact-horizon endpoints.

ISIN is identity; ticker is dated. Five sourced same-class links are admitted: SSBR→ALSO, ALSO→ALOS, ARZZ→AZZA and TRPL4/3→ISAE4/3, with separate effect/knowledge clocks. Public prebirth coordinates stay empty; internal history follows only the dated ancestor chain. Acquired ALSC/SOMA/Natura holdings are distinct from surviving-company histories. Spot links do not authorize loan aliases, options streams, custody permissions or unrestricted issuer vintages.

StageC source admission adds fourteen corporate security events. Accepted matched data adds BKBR/ZAMP, RRRP/BRAV, TIM's reverse merger, old-JSL/SIMPAR and the separate JSLG11/logistics-JSL episode. Old holding JSL history goes to SIMPAR; it must never become new logistics history when BRJSLGACNOR2 reopens November11 2020. Backward history walks traverse strictly decreasing event times. Private current-episode lookbacks and historical public market/peer returns are separate; index portfolios use their disclosure-date identity episode. Market continuity across legal issuers does not authorize pooled filings/capital units; SIMPAR/TIM have separately sourced own legal identities. Four continuations restore240eligible days and retire24stale cells. Every remaining feature/target dependency and complete store qualifies;2,400new live daily-peer losses and17BRAV utilization losses retain original rules. Only RRRP provides predecessor M1;180restored eligible days remain without intraday observations. Gross targets gain1,389; final neutral targets gain1,534, including145existing outcomes with restored risk support. Reuse all source/reducer/endpoint controls and272combined full933/full60 consumers; no repeat campaigns before fresh compatible fits.

The [surviving-company proof](docs/v2_SURVIVING_RENAME_PROPAGATION.md) preserves55ALSO/60AZZA restored eligible days under unchanged liquidity rules. SSBR/ARZZ have no predecessor M1 stream; missing history stays missing. All peer/share-unit uncertainty barriers and enumerated live support losses remain. Do not relax thresholds, drop names or reopen source censuses to erase them. Round7 continuation quotes are valuation/exit evidence, not daily-feature backfill permission.

Financial inputs use each filing's own version, original receipt, period/basis, currency and capital-unit scale. Later revisions, adjacent filings and modern viewer labels cannot backfill earlier facts. Unit uncertainty may invalidate a denominator while unrelated accounting fields remain usable. Completed original-source audits and their publication/revision limitations remain binding.

Primary labels are the on-demand characteristic-neutral target using gross shareholder returns and final sigma/slow risks. Physical ranks, price returns, gross entitlements and funded returns remain distinct. Corporate basket labels require observed exact endpoints for every nonzero share leg; unknown lot-dependent post-auction outcomes stay unsupported. Optional to-close loss remains inactive.

## Economic contract

The [corporate account](docs/v2_CORPORATE_ACCOUNT.md) uses R$10m primary and R$1m/R$5m checks. Primary100% historical CDI on eligible settled short proceeds and brokerage0 are negotiated-account hypotheses, not obtained quotes. Separate dated B3 spot/loan/custody charges, rent/intermediation, brokerage, shortfall, free/proceeds income and actual debit financing. Never add B3 atop the old bundled4bp, import punitive retail tolls or grant ordinary-CNPJ fund discounts. No-interest proceeds is not required admission. Historical and prospective current terms stay separate; sensitivities change one factor.

[Historical costs](docs/v2_HISTORICAL_COST_COVERAGE.md) and [invoice closeout](docs/v2_SPOT_INVOICE_CLOSEOUT.md) bind dated spot, custody, maintenance, physical-stock and invoice contracts. Unrounded fractional research units remain primary. Client payment/valuation/grouping are explicit hypotheses. Exact client invoices or broker quotes are not invented prerequisites.

Stage C's actual SULA-unit fraction sensitivity exposed a physically due partial return retained until close interest/payment. Provisioned fractions now use only unreturned cohorts, leaving due records unchanged for normal conversion/accrual/payment. Future returns and duplicate unreturned roots remain guarded. Resolve stage_c_pending_fraction_qualification; all earlier successful books take the identical branch, and only three newly exposed capital books received saved-intention/Decimal qualification.

Both independent and differentiable accounts preserve signed shares, pending spot cash, restricted proceeds, physical custody, claims and loan cohorts. Funding uses prior-close settled balances and the same corrected CDI as the benchmark. Missing quotes never fabricate liquidation. Known morning payments precede decisions; new same-day information/fills realize afterward. Recognition differs from payment; terminal obligations remain. Existing independent SAM/TBPTT copies and gradient proofs are reused.

The [loan contract](docs/v2_LOAN_ACCOUNTING.md) preserves original references/rates/fees, historical minima, pending returns and sourced corporate transitions. Primary assumed-approved renewal is63B3 sessions, four early;30/126-session, denial, recall, grouping and precision contrasts already qualify. Published averages/balances are proxies, not guaranteed locates or capacity. Never infer a successor loan reference or renewal from spot identity.

The canonical economic_account/corporate_replay now select the exact StageC composition already used in qualified portfolios; resolve stage_c_account_composition and docs/v2_matched_account_acceptance.json. Primary configuration and cash/lending sources remain exact. Earlier integrated account, terms and frozen experiment bindings remain immutable. Held-cohort identity transfer is an explicit bookkeeping hypothesis; no loan-source alias or new physical receipt is created. ALSC whole claims stay locked while credit/net cash are unknown; ENAT auction stays null. Cielo cents and opposite spot-fill daytrade treatment require actual exposure; never force holdings by changing preferences/rates. Preserve measured adaptive fixed-fee uncertainty. Synthetic book contrasts are not model alpha.

Qualified runtime supports sourced source-identity reopening and unit/no-cash rekeying of a pending claim's underlying, including ENAT/RRRP/BRAV. Effect-day delivery for JSL/VIVT/MODL/VVAR/SULA and loan-fraction conventions remain explicit hypotheses with completed actual-portfolio sensitivities; LCAM/TIM delivery dates are sourced. The corporate terms retain their original producer bytes and provenance; the matched account acceptance supplies their later admission without another numerical campaign. stage_c_event_replays binds100corrected/60reused old-coordinate books; stage_c_sensitivities and stage_c_account_results bind saved sensitivity arithmetic and conditional model comparisons. No loan cash redemptions or opposite security/day spot fills occur in these new paths; conditional Cielo/daytrade limits remain. Old fits and the accepted model store are unchanged.

December30 2016 and November20 2017 have source-supported clearing dispositions. January25 2017 remains historically uncertain; [qualified calendar hypotheses](docs/v2_JANUARY_CALENDAR_ACCEPTANCE.md) preserve the closed-day primary and separate adjacent-close cash/delivery endpoints plus one extra loan interval. Reuse the15 qualified variants/three saved controls; no fake equity sessions or all-money-date substitution. Corrected CDI already includes all monetary observations.

## Subsequent work and delivery

After integrated A, C compares registered matched attention, wider attention, GRU early-peers and C6 economics separately from data refits. Two named leads may replicate on ten other development folds only under corrected four-fold admission. This is explicitly reused development. Net-CDI is primary; IC diagnostic. Preserve paired20/40/60-session uncertainty, seeds, currency-consistent Sharpe/drawdown gates and complete trading statistics.

Width recovery selects imported runtime by arm and preserves completed TE128 work.
Qualified depth is separately available in economic-accounts@734cf12 and LSTM in
lstm@c37b432. Clean derived checkouts normalization-depth@a9a775c and
normalization-lstm@67629a7 add only the qualified compiler correction; their
model/temporal engineering bindings remain unchanged. The freezer binds and
checks the qualified compiler bytes. Depth is frozen/running; LSTM is not yet frozen. ops/run_capacity_layers.py
--wave depth|lstm reuses the frozen width fit loop; ops/admit_capacity.py applies
the original economic screen. Never change a runtime while its worker is active.

D begins attention96→128 and GRU64→96, then distinct temporal/recurrent depth. LSTM/peer/context/general capacity follow registered conditions. Maximum two new candidate cells per GPU wave, frozen contrasts and compatible parents; no grid, automatic extra seeds or reduced populations/history/budgets.

Archive/hash recoverable new artifacts on D without immutable-input duplication, verify recovery, update pointers, commit/push and verify GitHub. Temporary recovery copies reclaimed for storage are enumerated by exact-byte receipts; original audit views/archives/accepted data remain. Publish the combined report only after all stages finish or explicit registered conditional stops are documented.

Python3.12/uv in research/, Polars/PyArrow/NumPy, targeted high-value checks and Ruff. Keep code lean. Immutable boundaries: quant-data/b3/raw/**, canonical source archives and Trading/**. Resolve pointers; do not recursively scan all data. [Foundation](docs/v2_FOUNDATION.md), progress and [historical context](docs/PROJECT_CONTEXT_HISTORY.md) preserve older decisions; historical permissions do not authorize new work outside this contract.
