# Original B3 numerical sources and corporate basket targets

This checkpoint completes the missing original option/cash numerical boundary
and admits a bounded corporate endpoint amendment. A/B remain incomplete;
C/D are unstarted. No model forward, scoring, corrected profitability or GPU fit.
Accepted stores, original sources and old fits remain unchanged.

## Original IN/PR reports

Resolve `bvbg_source_number_audit`. The independent reader uses direct XML paths,
original nested ZIPs and exact dated instrument IDs; it does not call the
production selector, parser or feature reducers. All **1,283** admitted publication
jobs pass **6,334,727** field comparisons: **210,071** OI aggregate rows and
**555,511** cash rows. It checks **41,371,764** listed option series across snapshots
and sums **18,676,933** observed OI series using exact integer units. Missing OI
stays missing; complete coverage is distinct from observed-subset coverage.

Selected XML creation times, versions after the decision cutoff, report dates,
listed trading/expiry limits, underlying instrument joins and BRL monetary
attributes reconcile. All retained price factors are one; allocation round lots
include 1, 10 and 100. A round lot is not a multiplier on the printed OI quantity.
No ticker or option-series alias is inferred across a spot rename.

The original January 4, 2021 malformed-latest-publication disposition and the two
June 2021 moved/early-report dispositions remain explicit. The early June 11 cash
report is not admitted as a completed cash session. December 8, 2023 cash uses its
already bound exact-date COTAHIST identity when IN is absent. Retained publication
archives and their original-source receipts are preserved. Four initially relative
raw paths were resolved against the owning manifest; their hashes/scope did not
change, and the initial failures are retained.

Eight CPU workers completed the final invocation in **1,273.50 seconds**, reusing
six prior passed jobs; aggregate worker time is **10,138.18 seconds** including
those jobs. These are source-audit timings, not fit estimates. Per-publication
receipts allow resumption without repeating completed work.

## Printed option quantities and auxiliary cash axis

Resolve `option_quantity_source_audit`. Independent fixed-width slices reconcile
**9,052,797** original call/put records from the fifteen bound 2010–2024 COTAHIST
ZIPs into **261,904** aggregate rows and **785,712** quantity/count comparisons.
The explicit source underlying ISIN and all accepted dated intervals are used;
no current ticker, inferred multiplier or zero-trade day is invented.

The separate auxiliary cash axis reconciles **1,173,583** rows and **5,867,915**
value/unit comparisons against the already audited annual equity normalization.
The original quote census is not repeated. The normalized `close_brl` has already
applied its quote factor; the audit's initial schema-name and second-division
errors are retained as harness failures. All fifteen completed option scans were
reused while correcting the cash comparison. Final cash resumption took **6.19
seconds**; original option-scan wall time was not separately recorded.

These audits establish fidelity to the preserved source vintages. They do not
measure unknown historical revision share, prove disclosure completeness, or turn
XML creation time into an independently observed internet-publication timestamp.
The existing next-decision convention and separate OI position-date dispositions
remain explicit. No source or feature value changed in these numerical audits.

## Corporate endpoints and the actual primary target

Resolve `corporate_target_propagation` and `corporate_target_input_audit`.
`targets.py` now supports sourced share baskets: each nonzero successor leg needs
an observed exact exit price; intervening missing quotes are harmless when the
action chain is resolved. Cash remains cash/receivable, without reinvestment,
funding or execution costs. Subsequent successor q/cash events remain separate.
Custody delivery does not postpone economic succession. No basket high/low is
invented from nonsimultaneous leg extrema.

The **33-date/all-933-name** old control reproduces **1,693,560** cells exactly.
Its first attempt used downcast stored q/cash and disturbed 79 rank cells; the
qualified reconstruction uses the original Float64 verified-term table plus the
already admitted rename links, without rebuilding raw sources or daily features.
The initial executed recipe and failure are retained.

Independent closed forms verify **21** crossing outcomes each for BRML, Dommo and
Copel: **63** gained, none lost. Copel retains both 1 ON and 4 PN; source cash and
successor prices retain their precision. All actual endpoints precede fraction
auction recognition, so integer investor-lot rounding cannot be inferred here.
Post-auction unit outcomes remain unsupported until a quantity-specific contract
exists. Cielo's later shareholder cancellation adds no supported crossing outcome
under the existing exact-quote endpoint rule. Loan-redemption terms never become
shareholder target cash.

The physical rank target changes 8,243 cells, normalized residuals 6,322 and price
ranks 8,249. The **actual registered primary** is the store's virtual characteristic
projection, not that physical pre-neutralization array. Holding repaired risk
characteristics and sigma fixed, it gains **63** valid values, loses none and
changes **8,186** existing valid values. Total numerical changes are **8,248**.

The first consumer audit omitted sigma, so the virtual target was unavailable;
this was an incomplete audit view, not a missing production target. The qualified
dataset/collator checks **33 full-population/60-session samples**, **4,310,460**
target/mask cells, and 33 revoked endpoint windows, with zero mismatches.
An independent SVD projection verifies the registered nonlinear risk groups,
linear fallback, clipping and numerical tie convention. No neutralization rule or
loss changed. Consumer runtime **12.76 seconds**; bounded producer **0.71 seconds**.
Sixteen affected target tests and Ruff pass. Future-price prefixes remain exact.

## Remaining boundaries

Each converted source still has six liquidity-based eligibility sessions after
effect, although its own quotes cease: **18 cells** in total. The initial producer
receipt's contrary narrative was corrected from its already accurate case counts;
no arrays or checks were repeated. These names/cells were not deleted. Their
claim-close valuation and any slow-feature/eligibility consequences still require
explicit treatment in the complete derived-store contract.

The current changes are intermediate sparse target amendments. Complete corporate
wealth/label scope, any remaining upstream-clock qualifications, combined final
neural tensors and an explicit refit contract remain. Stage A's adaptive-book
renewal/recall, disposal, invoice/minimum/grouping, calendar and event sensitivities
are still required before final A/C acceptance. Earlier histories, feature families,
native/scalar inputs, cash-clock and AERI amendments must all be assembled without
silently supplying changed coordinates to old checkpoints.
