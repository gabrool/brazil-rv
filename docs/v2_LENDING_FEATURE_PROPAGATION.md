# Recovered lending observations in new feature coordinates

This is intermediate Stage B, after `b8dfd19`, using the sealed 3,717-session /
933-name axes and the source-bound rename eligibility amendment. It changes no
accepted store or old fit. Resolve `lending_feature_propagation` through the
economic/data run pointer. Accounting-only replay still uses frozen policy
features; these changed lending inputs require the new derived-store/refit contract.

## Source union and units

The previous **feature** archive contains 242,232 balances and 47,836 rates. It
is larger than the former **economics** archive. Replacing it with the recovered
230,454 balances would discard 13,501 already accepted observations. The new
243,954-row union retains every original feature balance exactly and adds 1,722
qualified observations. The reconciled 202,698-rate archive covers every old
feature-rate key; 29,661 old keys change rate, quantity or contract count. Taker
and donor averages remain separate, quantity-weighted, modality-blended proxies.
Registered flow can include manual renewals; it does not identify new contracts
alone, an executable quote or our intermediation commission.

Two existing original-source dispositions survive the otherwise successful
printed-row census. On July 15, 2020, the monetary column lacks consistent decimal
separators: all 267 mapped monetary values remain unknown while their quantities
remain usable. The GMAT3 October 15, 2020 row prints quantity 10 and amount
9,070,000; both remain unsupported. No scale is guessed, no security is removed,
and the printed rows remain in the immutable source and recovered audit. These
are the exact prior field dispositions, not blanket exclusions for failed dates.

The separately bound economic archive reuses the recovered rate bytes and the
qualified balance union. Its borrowing availability is identical on all eligible
days to the prior recovered-economics panels; annual rates, imputation, placeholders,
strict and open availability are exact on the full axes. The early 2% economic
placeholder remains explicit. It does not become an observed neural rate feature.

## Feature effects and denominator history

Six original lending fields reproduce the previous active-date consumer on
184,222 rows before source changes. The first audit attempts compared inactive
rows and then null versus -1 unknown-age representations; those harness errors
and executed bytes remain archived. Both unknown-age forms produce the same
absent-age tensor. New reporting counts only nonnegative known ages.

| Field | Added usable eligible values | Changed shared values | Lost support |
| --- | ---: | ---: | ---: |
| Balance / ADV20 | 1,113 | 0 | 0 |
| Balance change, one source session | 1,520 | 0 | 0 |
| Balance change, five source sessions | 1,752 | 0 | 0 |
| Annual taker rate | 21,777 | 5,251 | 0 |
| Rate change, five source sessions | 24,504 | 8,594 | 0 |
| Registered-quantity surprise | 26,090 | 32,828 | 0 |
| Utilization | 84 | 0 | 126 |

Totals are 76,840 additions and 46,673 changed shared values versus the prior FCA
lending family, evaluated on the updated eligibility. Of the balance-field gains,
38 in each of the three fields come from admitted predecessor ADV history;
the other gains come from source propagation. No loan rate, balance or registered
flow is relabelled across the spot rename. GOLL's activity repair predates every
balance observation and cannot alter these later ADV20 windows.

The 126 utilization losses are specifically enumerated ALOS dates from
October 27, 2023 through May 2, 2024. FRE127027, known May 19, 2023, reports
532,365,440 shares measured March 21. The predecessor's observed distribution
number changes 102 to 103 on May 2. The existing capital-unit uncertainty rule
therefore blocks the old denominator; the new ISIN previously erased that check.
The implementation now carries predecessor and successor barriers, bounded by
the decision's effect/knowledge clocks. This is **not proof of a split or of an
incorrect printed denominator**. Exact old ratios and selected sources are
retained, and a separately sourced resolution of that event may restore support.
Neither a guessed adjustment nor lowering a support threshold is justified.

The full producer saved its results before a too-strong no-loss assertion stopped
it. The qualification reused those outputs, independently established all 126
denominator cases, checked the future-publication deletion prefix through
December 28, 2023 and completed 195,930 actual family-loader cells with zero
mismatches. Its first loader fixture passed unfiltered dates to a restricted
calendar; the corrected fixture passed. Qualification took 3.26 seconds, not a
neural-fit estimate. Nine affected B3 feature tests pass, including the new
predecessor/successor barrier and late-knowledge fixture; Ruff passes.

## M1 boundary identified for the next repair

`rename_m1_support_audit` checks all 88 newly eligible dates through the actual
native-fast consumer: 91,080 values/masks/patch cells match and all permanent
security indices are correct. All 60 ALOS and 28 ISAE4 dates have current-day M1
streams. Both successors nevertheless have zero scaled-return/range channels
in their first 20 sessions; same-clock volume is available on only two and four
of those sessions respectively. Their predecessor history still needs propagation.

The diagnostic binds the exact existing raw source mappings and producer path;
it is not a completed M1 repair or raw-source census. Reconstruct only the affected
windows with the admitted identity clocks, original return-consistency tests,
same-clock support, 60-session model history and decision cutoff. Remaining
auxiliary histories, contractual wealth/labels, full derived-store/tensor acceptance
and Stage A execution/lifecycle bounds remain open. C/D have not started.
