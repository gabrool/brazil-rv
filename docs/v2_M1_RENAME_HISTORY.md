# Native M1 continuity at ALLOS and ISA renames

The bounded Stage B repair carries original predecessor M1 history into ALOS3
and ISAE4 only after the source-bound unit/no-cash rename is effective and known.
It preserves original source assignments, permanent indices, the full population,
60-session model history, decision-bar exclusion and every existing support rule.
No old fit is scored on these amendments. A/B remain incomplete; C/D have not begun.

Resolve `rename_m1_propagation` and `rename_m1_input_audit` through
`v2_economic_data_scaling_run.json`. The accepted store is immutable. The new sparse
native deltas and raw scalar amendments are intermediates for its explicitly
separate successor contract.

## Source and implementation

The two already accepted XP physical files contain dated predecessor and successor
segments. Bounded reads cover August21 2023–January2 2024 for the initial ALLOS
repair and September13–December30 2024 for ISA. An additional ALLOS tail reads
only the required 21-session warmup and remaining development dates through
December30 2024. No 2025/2026 bar or present-day payload is consumed.

`stream_intraday_from_assignments` retains the ordinary per-identity computation,
then recomputes a bounded continuation window. Up to 21 prior accepted predecessor
sessions supply the unchanged 20-session reducers and their prior-close checks.
Original assignment dates and physical missing-bar masks still apply. The overlay
is written only from the later of effect and knowledge through the finite history
influence window; pre-birth successor arrays and predecessor arrays remain intact.
No M1 stream is invented for ISAE3, which has no restored eligible native source.

The historical return-consistency inputs follow the original predecessor before
conversion. The q1/cash0 boundary uses the source-bound cross-identity official
return, never a current mark substituted for a missing reference. Unrelated action
boundaries and the 0.005 log-return consistency tolerance remain unchanged. Same-clock
volume still requires 16 of the preceding 20 sessions. Sparse missing minutes remain
unknown, including for activity. Native scale uses the already admitted daily
`target_scale_sigma`, without refitting or modifying its calculation.

## Effects and validation

| First 20 sessions | ALOS old → repaired | ISAE4 old → repaired |
| --- | ---: | ---: |
| Valid scaled-return patches | 0 → 1,329 | 0 → 1,321 |
| Valid scaled-range patches | 0 → 1,322 | 0 → 1,315 |
| Valid relative-volume patches | 116 → 1,174 | 228 → 1,302 |

Native validity gains **7,419 cells**, with **zero losses**. Initial native numeric
changes total11,253 cells (including newly valid values). ALOS's remaining admitted
daily-scale differences change28,287 existing numeric cells through the end of2024;
their relative scale discrepancy after the initial45-session window is at most
1.236e-6. That tail is propagated exactly, not discarded as immaterial. Patch counts,
current-day stream availability, signed close location, observed/elapsed fractions
and last-price-age channels remain unchanged.

Raw daily M1 scalar features gain71 ALOS and125 ISA values, with no losses in these
bounded windows. These **196 raw scalar gains are not yet final cross-sectional
transformed model inputs**. Their final transforms, ages, affected ranks and to-close
target assembly must be completed in the full derived store. The current native
input audit intentionally does not claim that boundary.

The bounded native controls reproduce243,916 sealed cells exactly, including the
warmed predecessor control. Future-source deletion preserves both repaired native
and raw scalar prefixes. A fixture independently compares the continued source to
an uninterrupted source; it tests delayed knowledge, restrictive original assignment
bounds, missing predecessor support, and mutations at/after the decision bar. Eighteen
existing affected intraday checks passed; the new case and one overlapping assignment
test passed after moving accidentally displaced test-tail assertions back to their
original fixture. That was a test-edit defect, not a source or model discrepancy.

The actual dataset and collator check **314 samples, all933 names/full60 sessions,
36,892,996 packed native cells**, zero mismatches. This includes all88 restored
eligible dates, the scale tail, and pre-event controls. Audit stores are bounded
input views, not accepted model stores or checkpoint inputs. No neural forward,
forecast scoring, GPU fit or profitability inference occurred.

Initial reconstruction took9.35s; the resumed input qualification took18.57s,
excluding its already completed tail source computation. These are audit timings,
not fit estimates. The initial audit expected69 patch slots from a local schedule
that requires68; the unused sealed padding was verified empty and the comparison
corrected. A later collator audit used obsolete fixture key names; its already
completed tail output was reused after fixing the readout. Executed initial and
qualified source bytes and initial outputs remain recoverable. Ruff passes.

## Remaining work

Finish final full-population scalar transforms/ages and to-close targets, remaining
auxiliary histories/joins and upstream clocks, contractual corporate wealth/labels,
and the complete derived store/refit contract. Stage A still requires the registered
adaptive-book execution, renewal/recall, custody-disposal and source sensitivities.
Native continuity is repaired; it does not imply completion of Stage B or permit
old checkpoints to consume changed coordinates.
