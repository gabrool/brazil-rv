# Natura source correction: complete data propagation

The complete development derivative is accepted for the narrowly sourced September18
2019 bonus q2 and November7 gross JCP .12784527353/share. Resolve
`v2_natura_data_inputs.json` or `v2_economic_data_scaling_run.json:economic_refit_inputs`.
The earlier `v2_economic_data_inputs.json` remains the immutable accepted baseline.
The new root is `C:/quant-data/b3/processed/v2_economic_natura_store_20260920`, manifest
`93f65b11f31979c6eafee0ca7e68afe5bb591a303eb8b5e2148072828e8d04bf`.
All106arrays,3717development sessions,933names,2009warmup and full60-session histories
remain. Original source retrieval, the252wealth amendments and42gross endpoint
proofs were reused. No original source census or prior store assembly was repeated.

## Scope and preserved contracts

The accepted prior action/wealth/target milestone supplies exact minute-end source
clocks and raw quotes. Daily wealth keeps the existing Float32 recurrence. The bounded
new reconstruction uses253prior sessions and stops after the first clean July2020
monthly peer refit plus three controls. It recomputes24wealth-dependent slow fields;
eight activity/raw-shape/history-age fields remain unchanged. All original support
thresholds, rank transforms, time lags and peer rules remain.

Natura's only accepted M1 assignment starts July2 2025. Its2019 native, scalar and
to-close support is empty. The scope audit verifies257726empty support cells over
the449-session source window without reading any original M1 file or held-out
observation. A changed own-name sigma cannot amend another name's native channel.
No2019 stream is inferred from the current ticker or shared ISIN.

Financial/valuation and lending use unchanged original filings, share counts, raw
prices, activity, dated identity and DISMES/detector uncertainty inputs. Those
definitions do not consume shareholder q/cash or wealth. Options, microstructure,
odd-lot and rebalance reducers retain their original raw observations and joins.
The old market detector remains a diagnostic; it is not proof of a split. This
correction does not silently remove the conservative denominator barrier or invent
a new financial feature. Macro shocks, feature-CDI, ADR and flow clocks stay fixed.

NATU->NTCO succession, new issuer inheritance and NATU/ALSC/SOMA account admission
are outside this two-scalar derivative. Bonus custody onSeptember20, JCP payment
February26 and withholding/loan compensation remain distinct account obligations.
Gross target cash remains unremunerated and unreinvested.

## Effects and qualification

The original24-field controls match8,777,664transformed values/masks. Monthly cluster
controls also match. All three clean-tail dates and the pre-effect decision agree.
The market-median/regression and peer-group dependencies propagate across names:

| Array or view | Changed cells |
| --- | ---: |
| slow values | 52,968 |
| monthly cluster labels | 5,076 |
| target sigma | 60 |
| common-state diagnostics | 9 |
| magnitude values | 191 |
| cross-market values | 1,114 |
| sector values | 0 |
| virtual characteristic-neutral targets | 14,039 |

No feature/target validity or age changes. Membership and all existing158active
support losses versus the foundation are unchanged. Shared raw reducer changes,
cross-sectional rank changes and target changes overlap; do not add those counts
as independent observations. Physical target_primary changes2576cells and normalized
residual3656; it is not the actual virtual primary. The42raw shareholder/price
outcomes stay the previously independently verified source changes. Final neutral
targets use the corrected sigma and slow risks with the independent SVD oracle.

Fifty new target-control dates verify2,566,000cells; the previous22-date control is
reused. The72-date target update supersedes the earlier risk-fixed deltas for final
composition. Do not apply both as independent changes. The qualified family controls
cover51,022,524raw/aligned/typed cells. Sector and magnitude producers were reused
after the cross-market precision correction.

The bounded daily dataset/collator verifies206samples, all933names/full60, and
2,241,413,076packed cells. The complete106-array derivative independently verifies
506,333,457changed-array/scope cells;85untouched arrays are hash-exact. Final combined
dataset/collator verification adds33samples and383,630,940packed cells across all
families/native inputs/actual neutral targets, zero mismatches. Two actual source-date
consumer read guards each check54nonfuture feature reads with revoked horizons empty.
The existing parent-transfer gate rejects the previous economic store before weights
are read. Three focused feature/causality tests pass; Ruff passes.

Action arrays change two q values, one gross cash amount and one payment-session
index. The known same-day boundary becomes true onNovember7; despite the legacy
array's unit-oriented name, its actual producer flags every already-announced event,
including cash. M1 support remains absent. Two alignment-role rows are replaced;
other inherited diagnostic/source rows, including the prior AERI disposition, stay.

## Attempts, uncertainty and timing

Initial qualifier report serialization rejected a NaN mismatch example after saved
outputs were written. The reportable qualification reuses the raw reducers. Eighteen
unrelated inactive raw-risk cells differ between the scratch reconstruction and the
accepted baseline; both scratch scenarios agree there, all Natura/eligible risk
controls match, and final composition preserves all18accepted NaNs. Every cell is
listed in `preserved_offscope_sigma.json`. This is a bounded reconstruction limit,
not an asserted new source defect or permission to expand their support.

Initial cross-market products were compared asFloat64 before the original
panel_frame Float32 cast:332232numerical mismatches, no mask/age mismatch. Applying
the original cast to saved outputs makes the control exact, without repeating
regressions. Initial executed recipes and outputs are retained. Two read-only
inspection errors (nonexistent guessed filenames and console Unicode rendering)
did not produce or alter data. No source download or neural execution occurred.

Successful daily producer27.22585s; reportable transform qualification2.93664s;
daily/target consumer29.91119s; auxiliary producer113.76432s; typed qualification
1.90873s; assignment/dependency scope.18741s; complete assembly49.10546s; final
verification19.98663s. These are audit/assembly measurements, not fit estimates.
The existing observed-vintage/revision/publication limitations remain.

New P conditioning and weights are required on this exact store; only compatible
new F children inherit. Preserve graphs, folds, seeds, learning budget, optimizer,
selector and full history. The optional to-close loss remains inactive. Accounting
and rate/availability replays preserve frozen old PolicyData, with separate data/refit
attribution. Stage A and final economic admission remain incomplete; C/D unstarted.
No corrected model profitability or final-program completion is claimed. Recovery
and its dependencies are bound by `natura_store_recovery` in the economic run pointer.
