# Surviving-company identity and dependency progress

2026-09-20. This is work within the existing corporate/calendar/source delivery
block. Stage A and this block remain incomplete; C/D have not started. Resolve
`surviving_rename_progress`, `surviving_rename_dependency_progress` and their
evidence pointers from the run pointer.

Original issuer notices703585 and1264889, already retrieved and visually qualified,
support SSBR→ALSO on August6 2019 and ARZZ→AZZA on August1 2024. Minute-end public
availability is August5 21:20UTC and July31 21:21UTC respectively. These are the
surviving issuers' ordinary shares under new identifiers: unit quantity/no cash,
separate from acquired ALSC/SOMA exchange ratios. The code allowlist now contains
five links. The existing three links retain their economic/source clocks; the
later ALSO→ALOS continuation root now traces to SSBR. No acquired-company history,
new loan alias, physical-delivery permission or unrestricted issuer vintage is
admitted by this spot identity evidence.

The actual unchanged prior-session liquidity rules add55ALSO and60AZZA eligible
days, retiring six stale ARZZ cells. ALSO's first five sessions still fail the
thresholds; the former zero-first60 inventory was not proof of60valid days.
Two61decision controls/244cells and independent direct prior-window arithmetic
pass, including delayed-knowledge exclusion. All933names remain. Accepted stores
and source assignments are untouched; sparse amendments are intermediate only.

The original Float32 wealth recurrence resumes from the accepted predecessor
close. Selecting2,694 normalized source rows verifies7,250 unchanged standalone
successor cells exactly. Both new chains differ through December2024, including
the SSBR→ALSO→ALOS chain, so the numerical tail is retained. Internal prebirth
lookbacks are distinct from public successor coordinates. No quotes, basket OHLC,
loan references or model forecasts are invented.

The daily producer uses253prior sessions and full933cross-sections through2024,
retaining252-session fields,60-session risk,126-session monthly peers and original
support thresholds.545,310 normalized source rows supply original Float64 price,
volume and trade coordinates; no original raw census is repeated. The first
producer saved both cases in123.6854401seconds. The unchanged controls exposed an
audit-harness mismatch: accepted market-array rows are not interchangeable with
the original normalized daily feature source. Its absent rows retain the original
missing-OHLC and certified complete-session zero-activity conventions. Restoring
source masks reused all expensive reducers; only four activity fields were
recalculated in5.358068seconds. Exact failed/qualified code, reports and qualification
stdout remain available. The canonical producer contains the source-contract
correction. No accepted data or research runtime was changed.

A bounded saved-mask check identifies all22,660source-mask rows and the same
22,660activity rows as already tagged `continuation_quote_mask` observations.
They are the sealed Round7 BDI06/07/08 valuation/exit extension, which deliberately
preserves original feature/universe coordinates; see [that contract](v2_ROUND7_IMPLEMENTATION.md).
The two lists overlap completely. This resolves the reconstruction mismatch's
provenance without reopening the continuation-source census or changing its
admission scope. The read-only method and source bindings are retained in
`v2_surviving_source_lineage.json`.

The final94checks cover83,150,470cells with zero mismatches. Daily amendments
change705,221slow values and6,103ages, adding5,176valid field cells and removing682.
All losses are enumerated:62are retired predecessor cells;620remain eligible but
lose peer fields under unchanged minimum peer support and changed causal clusters.
No support threshold is relaxed.1,317public risk-scale cells and11,944monthly
cluster coordinates change. Counts overlap and are not independent observations.
Unrelated inactive sigma reconstruction differences remain explicit and retain
their accepted values.

The actual dataset and collator pass185full933/full60slow-only samples, including
all restored eligibility days, both event boundaries, later monthly boundaries,
the chained2023rename and the development endpoint.2,009,467,410packed cells agree
with an independent per-destination ancestor walk; future feature reads are
rejected. This27.7133216second check is not a native/scalar/target audit, neural
forward pass, fit or model profitability result.

The surviving issuers' common state, financial/event, sector and lending
dependencies now qualify, together with microstructure, options, odd-lot and
index features. Two legal issuers retain their own CNPJ, ordinary-share class and
selected filing versions. Eight identity rows are added and eight stale inactive
rows removed; three additions are eligible. All shared identities remain exact.
Only those two issuers were reconstructed from the already audited financial
caches: 156,369 financial and 46,578 event control cells match exactly. There is no
new extraction, acquired-company history pooling or unrestricted issuer vintage.

Lending reuses 1,754 selected balance rows, 686 rate rows and 28 free-float records.
All 26,852 old-coordinate controls pass. Rates and registered flows on shared
keys remain exact; the economic borrowing archive is unchanged. Spot history can
support prior ADV and denominator-unit checks, but cannot supply a new loan alias.
The independent ancestor/date-selection audit identifies four existing DISMES
transitions: SSBR April30 2019 (108→109), and ARZZ July3 2023 (144→145), November13
2023 (145→146), July11 2024 (146→147). These are uncertainty barriers, not proof of
a split or an erroneous printed denominator. They affect 181 previously usable
utilization rows and 74 AZZA dates with three raw valuation fields. Only 15 of
those valuation dates were already eligible; therefore the composed store loses
45 live financial cells, not 222. Every lost value and selected source interval is
enumerated. No threshold or source observation was changed to restore support.

The eight families' final sparse effects against the accepted Natura store are:

| Family | Valid gains | Valid losses | Losses still eligible | Shared valid numerical changes |
| --- | ---: | ---: | ---: | ---: |
| Financial | 1,430 | 56 | 45 | 15 |
| Events | 805 | 7 | 0 | 0 |
| Lending | 365 | 187 | 181 | 0 |
| Sector | 1,230 | 3 | 0 | 13,831 |
| Microstructure | 175 | 2 | 0 | 0 |
| Options | 240 | 4 | 0 | 0 |
| Index | 44 | 12 | 0 | 0 |
| Odd-lot | 230 | 2 | 0 | 0 |

All 47 other losses are retired predecessor cells. Counts are feature cells,
not independent source observations. Common diagnostic state changes 397 values
without a mask change; it does not become a new model input. Sector calculations
retain their original two-other-issuer support rule, distinct from daily peers.

Activity/options use bounded 22-prior/22-following session sources; odd-lot keeps
its original publication and five-session lag. The 2019 window has no IN/PR or
option-quantity observations; none is invented. The 2024 window retains observed
subset OI, without full-OI/delta/uncovered-call support. Both original index preview
and last-effective holdings follow the dated spot identity; this is separate from
option-series or loan identity. Saved controls cover 2,304 activity and 7,366,968
index value/age cells. Independent arithmetic verifies 414 activity fields
(161 supported), 2,312 odd-lot fields and 22 actually changed/new index rows.
Future-source deletion prefixes pass. Earlier raw-source censuses are reused.

Final typed old-coordinate controls cover 158,241,768 cells with zero mismatches.
The actual dataset/collator passes 303 issuer/context/lending samples and 124
auxiliary samples, each with all 933 names and full 60-session history. The checks
cover 26,575,524 and 4,627,680 new-family sample cells respectively, with a second
comparison after collation. The two date sets overlap. Independent typed auxiliary
formulas check 32,651,268 value/mask cells. Previously qualified slow history is
reused without counting another slow audit; native/scalars and targets are not
qualified by these views. Actual future-feature reads are guarded.

The first context attempt saved common-state results before an alignment error
outside the bounded sector window. Its exact code/stdout remain; the resume reused
common outputs and ran sector reducers once. The independent auxiliary oracle
initially used Float64 division where the accepted NumPy2 producer has a Float32
scalar boundary. Its failed code/stdout are retained. The qualified oracle preserves
that boundary and records the wider reference separately; no produced data changed.
Completed activity/odd-lot arithmetic was reused; its counts were reconstructed
from saved outputs and the demonstrated failure boundary, explicitly labelled.
An event-date variable shadow in that audit was corrected in the resume. No
producer or consumer was repeated to fix these audit issues.

Producer times were 20.0149454 seconds for issuers, 25.667431 for the context resume,
2.388231 for lending and 4.5317548 for auxiliaries. Independent unit-history and
resumed index qualification took 22.6558876 and 7.753859 seconds; the two actual
consumer checks took 21.7621551 and 9.2228893 seconds. Initial failed full-invocation
wall times were not separately retained. These are CPU audit times, not fit ETAs.
Research runtime is unchanged. Exact executed recipes, attempts, frozen plans and
the pre-update registration/closeout snapshots are retained.
Resolve `surviving_rename_dependency_recovery` for the restored/hash-checked
new evidence archive; it extends the prior rename, source, lending and accepted
store recovery dependencies without duplicating immutable inputs or old fits.

Next reuse these saved proofs and deltas to finish native M1/scalars and affected
magnitude/cross-market dependencies. Combine the already-qualified72succession outcomes with
final risks and new identity/eligibility target effects into a new complete store.
January25 2017 clearing still needs its separate cash/delivery/rent disposition,
followed by integrated Stage A admission. Both earlier complete stores and every
old fit remain sealed. Existing V31–V41/cost/event/source proofs must not repeat.

Recovery verified for implementationcbbd77b: `surviving_renames_cbbd77b.zip`,
SHA `8276456df297755a3113f326a5ac9d72a3af15d6b632e31018e217f6b3167456`,
275,958,720bytes.215unique/241logical members and26aliases restore and hash-check;
28array files/158,397,482cells reconstruct to exact byte hashes from sparse
amendments and the immutable Natura parent.13.7534755seconds. New reducers,
failed/qualified/executed recipes, source subsets, config, docs and patch recover
with prior event/Natura/economic/held-source/lending dependencies. No full immutable
input/store/old-fit copies are included. Resolve `surviving_rename_recovery`.
