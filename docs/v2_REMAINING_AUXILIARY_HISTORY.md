# Activity, option, index and odd-lot rename propagation

The remaining four auxiliary families now follow the admitted ALLOS/ISA share
history on all 3,717 development sessions and 933 permanent axes. These are
verified intermediate amendments, not a replacement accepted store. Old fits,
source assignments and raw files remain unchanged. No neural forecast was run.

The activity builder uses only the bounded predecessor/successor source windows:
22 prior sessions and 22 following sessions around each rename. It preserves the
original 20-session volume/trade reducers, five-session ratios, missing-support
rules and source publication dates. Internal predecessor rows enter the successor
output only after both effect and knowledge. The same rule carries odd-lot share
history through the exact five-exchange-session, publication-vintage join. Later
predecessor rows do not refresh successor history. ISAE3 has its own share history
but remains ineligible under the existing liquidity rule; no option stream is
invented for it. The GOLL activity already existed in this separate auxiliary cash
archive, so the earlier activity-only correction needs no second patch here.

| Family | Added valid field cells | Retired predecessor cells | Existing valid numerical changes |
| --- | ---: | ---: | ---: |
| Microstructure (2 fields) | 176 | 4 | 0 |
| Options (7 fields) | 350 | 8 | 0 |
| Index preview (2 fields) | 76 | 0 | 0 |
| Odd-lot (2 fields) | 176 | 4 | 0 |
| Total | 778 | 16 | 0 |

No still-eligible valid field is lost. Every array outside the dated rename scope
is exact. Of the non-index additions, 126 microstructure, 296 option and 162
odd-lot cells come from restored eligibility on original per-ISIN source history;
50, 54 and 14 respectively need inherited history. The observed-subset OI fields
gain support, while complete OI, its delta and uncovered-call share remain unknown
on these paths. A share rename does not identify an option-series or loan alias.

The index producer had an additional identity defect: a preview keyed to the new
ISIN was compared with an unchanged old composition keyed to the predecessor.
Both compositions now route through the exact sourced link at each decision,
after its effect/knowledge clocks. The pressure proxy still includes price drift
and is not an estimate of actual passive-fund orders. Of 38 restored name/date
rows, 27 have enough standalone ADV history; their pressure values also require
the prior-composition identity correction. Eleven more require inherited ADV.
Each row supplies pressure and event age, accounting for the 76 field additions.
The initial consumer receipt used an inactive-row-filtered parent parquet as the
index eligibility control. `remaining_auxiliary_source_audit/index_attribution`
supersedes only those attribution counts using the already produced matched
eligibility-only panel. The canonical reproducer is corrected; passed arrays and
consumer checks were not repeated.

The bounded old activity controls match 3,564 value/age grid comparisons; the
index tail control matches 1,615,956 comparisons, including unsupported cells.
Independent arithmetic checks 621 bounded activity fields, including 322 supported
values, and 702 odd-lot fields. Thirteen targeted tests pass, covering delayed
knowledge, missing support, exact lag, future deletion, overlapping identities and
both-composition routing. Ruff passes. The actual dataset and collator check 90
full-population, 60-session samples, including all 88 restored eligible dates and
two preceding controls: 6,549,660 packed field cells, zero mismatches. The complete
four-family array/scope qualification examines 135,250,479 cells. These CPU audit
views are not accepted model stores or an authorization to reuse old checkpoints
on changed coordinates.

Independent original-workbook parsing reconciles all 44 admitted portfolios and
12,712 ticker/quantity/weight rows. It reads theoretical share quantities and
printed percent weights directly from original worksheets, without calling the
production portfolio parser. Older `IBRX` sheet naming, the alphanumeric `B3SA3`
code and macOS ZIP metadata caused audit-reader failures; each was corrected,
with executed versions retained. The 36 completed workbooks were reused after
the final ZIP qualification. Eight original nested IN/PR ZIP header checks across
the four pre/post-rename source dates reproduce the selected creation timestamps
and first permitted decisions. They verify original report existence, not delivery
to a particular broker. Single-vintage historical revision share remains unknown.
Earlier source-clock proofs remain bound dependencies; no raw archive census ran.

Producer reconstruction took 4.48 seconds and the array/consumer qualification
26.45 seconds. The final source-audit resumption took .58 seconds, excluding its
already completed reducers and 36 workbooks; it is not the runtime of the whole
source audit. These are not neural-fit estimates. An initial producer inventory-key
error is preserved separately; the completed rebuild and tensor audit were reused.

Resolve `remaining_auxiliaries`, `remaining_auxiliary_input_audit` and
`remaining_auxiliary_source_audit` from the economic/data run pointer. Final store
assembly must apply the qualified sparse four-family deltas together with all
previous history, issuer, lending, native/scalar M1, target-clock and AERI changes.
Source-specific original option/cash numerical reconciliation beyond the bounded
normalized histories, remaining corporate wealth/labels, final combined tensors
and the explicit refit contract remain open. Stage A still needs adaptive-book
lifecycle/execution bounds. A/B remain incomplete; C/D have not started.
