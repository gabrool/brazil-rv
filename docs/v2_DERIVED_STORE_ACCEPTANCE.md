# Complete economic-program derived store

The verified data amendments now compose into one complete development store.
Resolve `docs/v2_economic_data_inputs.json`; the foundation pointer and every
old fit remain unchanged. This is data acceptance, not economic admission or a
claim of profitability. Stage A lifecycle/execution bounds remain; C/D have no
new results or GPU fits.

## Converted-source disposition

The six liquidity-lookback sessions each for BRML, Dommo and Copel units remain
model eligible, with all prior history. Their source OHLC and coherent
reinvest-at-close wealth masks remain missing after conversion. The first
decision uses the final pre-effect OHLC; later return/volatility fields are
unsupported. Independently certified activity reducers and history ages retain
their existing support. Claim marks do not refresh those observations.

Eighteen separate closing entitlement rows reconcile to original source terms
and independently evaluated decimal arithmetic. They preserve cash receivables,
all successor legs and custody dates. Marks are available at the next decision,
and are neither source-security quotes nor a new model feature. No synthetic
basket highs/lows, reinvested cash or loan-redemption shareholder cash is added.
These six-session cases precede fractional auctions and contain no subsequent
successor q/cash action. Quantity-dependent post-auction and financed investor
wealth remain in the account ledger.

The explicit entry permission now closes from max(effect, knowledge), including
Cielo's separately known closed shareholder register. This changes **1,328**
permission cells over the full axis, **24** of which remain liquidity eligible:
the 18 converted cells and six Cielo cells. Every changed cell already lacked an
observed source quote. The independent quote execution gate and inventory
obligations remain. No executable fill or membership deletion is manufactured.
Resolve `corporate_claim_rows` for every term, mark and retained slow observation.

## Composition and verification

The store retains all **3,717 sessions**, **933 permanent names**, original
2009 warmup and full **60-session** history routing. The feature definitions and
schema hash remain exact. It includes:

- Corrected FCA issuer/financial/event/sector/exposure observations, plus exact
  ALLOS/ISA history and issuer clocks.
- Qualified lending union/features and the GOLL activity-only amendment.
- Both native M1 history and ALOS scaling-tail amendments, completed scalar
  transforms/ages, four remaining auxiliary joins and index composition repair.
- Dated auxiliary target normalization, AERI's rejected-factor wealth/diagnostic
  repair, all sourced basket endpoints and the source-entry barriers above.

Only six already produced family parquets require final typed alignment and
decision-local transforms. Their source reducers, financial extraction, raw M1
windows and original-source censuses were not repeated. The streamed assembly
took **63.30 seconds**. Its observed array counts are versus the sealed store;
they must not be added to incremental counts from intermediate overlays.

Independent reconstruction checks **1,798,674,885** composed-array cells, with
**33** unchanged arrays also hash-identical to the parent. Independent direct
source-row/field arithmetic checks **1,040,388,300** cells for the six newly
aligned families without using the production alignment or transform functions.
Both comparisons have zero mismatches.

The actual dataset and collator check **156** full-population/full-history samples,
including all 88 restored eligible rename dates, corporate windows, early history
and the final date. Fourteen samples are in the pretraining era. They verify
**1,856,671,832** array/packed CPU cells across slow values/masks/ages, all ten
sidecars, M1 scalars/native streams and targets. The registered primary remains
the virtual characteristic-neutral target; independent SVD arithmetic consumes
the final sigma and slow risk coordinates. No physical rank array is relabelled
as the neutral target. The optional to-close head remains inactive in the parent
recipe. Combined verification took **74.64 seconds**; no neural forward occurred.

Five further actual consumer checks delete future identity links in private
runtime views and reject reads of future feature observations. Earlier complete
samples remain exact, and revoked horizon payloads remain zero. The existing
parent-transfer admission rejects the old store before reading old model weights.
These boundary checks took **9.05 seconds**. Ruff passes on the four new recipes;
no unchanged source/model/accounting test suite was repeated. No failed executed
reconstruction occurred in this checkpoint.

## Support and causal limits

Versus the sealed store, eligibility changes only by the already admitted 88
rename gains and 12 stale predecessor removals. All 24 corporate liquidity cells
remain. Still-active feature losses are exactly **158** previously explained
observations: **68 ALOS utilization** cells within the prior 126-row capital-unit
uncertainty disposition, and **90 Embraer peer** fields under the unchanged
three-other-peer support requirement. The other 58 ALOS dispositions were newly
supported only in intermediate identity/eligibility overlays, so they were not
valid sealed-store cells. Every loss is enumerated. Remaining losses occur on
retired predecessor entries. No support threshold is relaxed or stock dropped.

Preserved vintages do not reveal unknown historical revision share, complete
unselected issuer disclosure, or independently observed first internet-publication
times. XML creation and next-decision conventions retain their stated limits.
Pre-NYSE SUZ remains unsupported; unresolved old lending rows remain unadmitted;
public average rates/balances do not guarantee locates or renewal. Prior source
audits and recoveries remain dependencies, not audits to repeat. Exposure-ranked
new corporate evidence, if found during Stage A replay, requires its own explicit
data attribution rather than an unrecorded store edit.

## Refit contract and next work

The new manifest is **not coordinate-compatible with any old P/F fit**, despite
unchanged feature definitions. New matched P training must estimate its own
training-only conditioning; only compatible new F children may inherit it.
Preserve the registered graph, full history, learning budget, optimizer, checkpoint
selection, folds and seeds. This checkpoint does not authorize a new loss or the
optional to-close head. The frozen recipe is in `v2_derived_store_contract.json`.

Accounting-only/source-only replays must still shallow-copy frozen PolicyData
and replace their explicitly admitted economic inputs. They cannot use this new
store as though it were the old model's coordinate system. Report accounting,
source availability/rate, changed-data and refit effects separately.

The next work is Stage A's adaptive-book denied-renewal/recall, pre-custody disposal,
invoice/minimum/grouping, timing/sweep and remaining exposed-event bounds. Corrected
matched C comparisons and conditional D capacity waves follow their registration.
No corrected profitability, replication admission, scaling outcome or final
program completion is claimed here.
