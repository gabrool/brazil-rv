# Compatible matched refits

The question is whether the existing model leads retain their economic advantage after correcting the historical accounting and model data. Stage C keeps the original graphs, seeds, folds, training budget, optimizer and checkpoint selection. The accepted matched store changes the inputs; new compatible P parents and F children are therefore necessary. Old weights never consume these coordinates.

Resolve `stage_c_refit_plan` and `stage_c_refit_root` in `v2_economic_data_scaling_run.json`. The frozen plan contains60 original-recipe fits:9 attention/GRU parents,36 children,3 C6 parents and12 C6 children. No width/depth contrast has started. Parent selection IC is a training diagnostic, not portfolio profitability or an adoption gate.

Training now runs in the clean detached checkout `C:/Brazil-RV/.worktrees/matched-refits`, commit12c659d3e4454f16df4dc96b1a080b68a71e7b7c. Its776 tracked research/ops files matched the main checkout byte-for-byte at isolation, including the frozen driver. `data_refits/isolated_checkout.json` records this. The earlier clean-worktree guard stopped before any first fit and later before the fifth parent began, after four completed parents; those four were reused. Exact worker logs remain. A raw-byte copy initially left EOL-only index status; staging refreshed the index without changing content or commit. The incorrect read-only preflight import and EOL operation are reconstructed tooling descriptions, not claimed standalone failed shell files. The still earlier source-event interruption before any saved epoch remains separately bound.

## Portfolio inputs and frozen evaluation

`stage_c_refit_economics` selects a new PolicyData cache for fresh forecasts. Original neutral calibration remains frozen. Allocation beta and residual variance consume61 wealth closes through t-1, yielding60 adjacent returns, with original40/20 support, Blume shrinkage, clipping,20-session beta carry and variance shrinkage. History crosses only admitted dated market links and decreasing event times. It never routes the reopened logistics JSL identifier into old holding history. This allocation risk is distinct from model slow beta/sigma.

The first producer saved all risk arrays, selected formula controls and hedge sidecar once. It then rejected an unused July18 2016 missing corrected-CDI prelude. The qualified47.13906-second resume reused the risk work and retained the old finite CDI only on that loader prelude. July19's prior-CDI feature inherits that explicit prelude; neither date enters an actual screen evaluation. All four actual windows have corrected current and prior CDI coverage. No invented monetary observation is admitted.

The cache uses accepted quotes, eligibility, entry permissions, prior references, qualified lending and original borrowing references. Eighteen distributions remain distinct from gross labels. Two already represented q1/no-cash JSL/SIMPAR and TIM source actions are removed from the scalar account arrays before their explicit custody/loan distributions enter, preventing duplicate economic conversion. ALSC physical credit remains unknown and locked; ENAT auction fields remain null. All original conditional loan/cost/source hypotheses remain.

`stage_c_refit_economics_qualification` binds exact cache-to-store/risk/source composition,1,977,027 independent backward beta-fallback comparisons, selected ancestor OLS checks and actual ledger arguments for501 screen sessions. Prior formula controls and2,800 original-cache comparisons are reused. The initial qualifier passed its array/risk/fallback checks, then failed serializing a NumPy boolean. Its first resume saved the small history records and passed action/distribution/cash composition, then the prior-CDI oracle failed to include the documented legacy prelude. The final1.55550-second tail reused those proofs and checked only the remaining corrected prior-CDI, references and actual fold arguments. Counts reconstructed from retained exact code/traceback boundaries are labelled; the unsaved fallback-use count stays null. Initial whole qualifier wall times were not recorded separately. No producer, historical book or model fit was repeated for these harness failures.

`stage_c_data_replay_plan` freezes96 fresh-forecast primary books: four models and four folds, each with three individual seeds plus an ensemble at R$10m, and ensemble checks at R$1m/R$5m. The actual original neutral allocator consumes the new ranks and qualified portfolio inputs. Readouts include paired net-CDI effects, three currency-consistent Sharpes, costs/income, turnover, exposure, drawdown, win/loss, forecast IC and observed holding spells. A spell is continuous signed stock inventory at session closes; partial resizing does not restart it, and an ISIN conversion ends that ISIN's spell. It is not FIFO or complete cross-corporate beneficial-claim duration.

Results attribute corrected data plus necessary matched refits against saved corrected-account/source books. They cannot isolate a hypothetical pure-data effect on incompatible old weights. Existing one-factor scenarios apply only where new actual paths expose them; prior mechanical proofs are reused. No fresh-fold economic result existed when this plan froze. The heartbeat remains paused; work continues directly.

The separately frozen `stage_c_refit_sensitivity_plan` reuses the105 prior phase/capital definitions on48 fresh ensemble books. A small new-selector check reproduces all1,680 previously saved inclusion decisions (1,062 exposed/618 skipped) without replaying an old book or charge calculation. It qualifies the changed selector only; those are not new model results. Cielo loan-cash exposure requires its previously frozen price bound if it appears. The common saved-book checker supports primary and sensitivity outputs and records every unquoted holding, prior debit and unresolved obligation before any adoption judgment.

Completed new checkpoint files may receive transparent LZX compression, with exact before/after byte hashes recorded in `data_refits/storage`. The first five compressed parents saved approximately31MB; every checkpoint remained readable and byte-identical. Twelve not-yet-started GRU child directories use empty junctions from their frozen C paths to D to share the available disk space. No existing data, original fit or active output was moved. `storage/path_layout.json` records each path. Training budgets and epoch-checkpoint retention remain unchanged.

Storage work subsequently reclaimed681 redundant reconstructed NPY copies (1,069,594,302bytes) from two explicit temporary recovery directories. Their hashes matched the recipes in unchanged, already verified archives before removal. Original audit views and all recovery dependencies remain. Transparent LZX compression of1,267 completed audit-view files reduced allocated storage from8,244,693,845 to685,965,312bytes, with every decoded hash exact and no OS failures, in115.05090seconds. These are storage checks, not repeated source/consumer proofs. Eighteen empty attention child paths for F6/F10/F14 also use D junctions; already running F2 paths remain in place. The exact scripts, file inventories and journals are bound by `stage_c_refit_storage`.

The first complete TE_full/F2 three-seed group produced six primary books on its first invocation. Saved accounting qualification passed for all six without a replay; the initial report is explicitly partial. No Cielo loan cash, opposing spot fills, prior debit or unquoted holdings occur in these paths. Remaining fit groups, model comparisons and actual-exposure sensitivities continue; one fold is not an economic conclusion.

The completed attention/GRU first-fold groups now have18 qualified primary books and183 qualified exposed one-factor books, with132 explicit skips. No primary book has unresolved or unquoted holdings, loan cash redemption or prior debit. Six unquoted sensitivity cells are the already admitted VVAR unit claim on November26–27 under its explicit delayed-delivery hypothesis. Nine recall2 books retain overdue loan obligations and unresolved economics; they are not executable improvements. Nine denied-renewal books do incur prior-close debit, over144 overlapping account-days. `stage_c_refit_debit_plan` therefore freezes the existing50/100annual-basis-point debit spreads against each actually funded, qualified denied-renewal baseline, with every other setting held fixed. This is a conditional one-factor financing contrast within denial, not a combined worst case or an amendment to the approved-renewal primary. Further completed folds enter only through the same exposure rule.

All18 first-fold funded variants completed on their first invocation in24.58086 summed case seconds; the unchanged saved-book checker qualified them in0.51934seconds through an isolated pointer context. Maximum independent NAV error is R$3.72529e-9 and debit-financing error R$5.68434e-14. NAV/target prefixes before the first actual debit and that day's intentions are exact. At R$10m the extra100annual-basis-point spread changes final total-path NAV by−.55699bp for full attention,−.30863bp for wider attention and−.44104bp for GRU, relative to their denied-renewal baselines. These are conditional adaptive portfolio effects over the first fold, not alpha or pure direct-charge estimates; all earlier adaptive uncertainty remains. No new runtime mechanism, source search, or old-book replay was needed.

The first F6 account invocation stopped in whole-window input validation before simulating any daily state: the portfolio-cache producer cleared the duplicate JSL/SIMPAR and TIM ordinary actions but set their successor indices to−1. The ledger requires each cleared ordinary action to retain its own index. The initial cache qualifier copied that same incorrect expectation. `stage_c_refit_action_identity` records the two-cell correction: September18 2020 JSL axis503 and October13 TIM axis825. The original cache, qualification, failed driver/producer/qualifier bytes and traceback remain preserved. The corrected cache shallow-copies PolicyData and its existing static coordinates; only the separate successor-index array changes. All other input and PolicyData fields retain their original object references, and all four actual account-window schemas now pass. F2/F10/F14 ledger coordinates are exact; F6 differs in those two indices only. No model-store or fit change is needed.

The first narrow correction checker omitted the separately passed shortable panel and used default account settings; it failed before writing a corrected cache. Its exact script/stdout remain. The qualified13.57658-second invocation supplies the actual primary settings and shortable panel, writes/hash-verifies the corrected cache, and advances input bindings through separate `qualified_plan.json` files. All original frozen plans remain. Hypotheses, driver bytes, model forecasts and fit budgets are unchanged; the completed F2 books retain their original bindings and are reused. No risk/source reducer or passed book was rerun.

F6 now adds18 primary books, each simulated once after that pre-simulation correction; all36 accumulated primary books qualify. Its nine ensemble/capital books have no unquoted holdings, loan cash redemption, overdue principal or unresolved economics. They do have actual prior-close debit:107 sessions for full attention,105 for wider attention and98 for GRU, at each capital. The frozen sensitivities therefore exercise financing on these actual portfolios. Another207 sensitivity books qualify in358.26179 summed case seconds, bringing the accumulated total to390 with240 unexposed skips. Saved qualification took8.48301seconds and reused every prior proof; maximum new NAV discrepancy is R$7.45058e-9. Eighteen unquoted cells are the already admitted JSL/SIMPAR claim on September18/21 under the two-session delivery hypothesis. Nine denied-renewal books have an overdue interval, maximum R$154.52286; this is separate from their terminal status. For example, full-attention R$10m is overdue only on September29 and has zero overdue principal at the final close. The whole-window unresolved flag is retained, with no invented buy-in or penalty.

The same frozen conditional financing rule adds18 F6 denied-renewal spread books, all first invocation,31.38480 summed case seconds; all36 accumulated funded variants qualify, with the new saved qualification taking0.87008seconds. Maximum new NAV discrepancy is R$5.58794e-9 and financing discrepancy R$2.84217e-14. All18 retain the baseline's whole-window unresolved distinction and have no unquoted holdings or loan cash redemption. R$10m extra100annual-basis-point spread final total-path effects versus denied renewal are−3.54478bp full attention,−3.63218bp wider attention and−3.32451bp GRU. These conditional adaptive effects are separate from the ordinary primary financing variants and are not resolved executable gains. Counts here are reconstructed from saved progress/qualification records; no book was replayed for this narrative. Read-only PowerShell pipe/path/output-truncation mistakes were tooling failures, not numerical failures or separately retained exact shell recipes.

The final result formatter now records dated overdue exposure and terminal overdue principal separately; an unexposed scenario inherits its baseline's exposure flags. `ops/replicate_economic_leads.py` is prepared and linted/compiled but neither frozen nor executed. It requires the complete four-fold result and explicit technical disposition, admits only the two registered positive-mean leads, and reuses compatible saved P parents for the other ten development folds. No extra parent, capacity or held-out fit is authorized by this preparation. Remaining screen fits continue uninterrupted.

F10 adds18 qualified primary books in34.07691 summed case seconds, with no repeated simulation, unresolved economics, overdue principal or loan cash redemption. Maximum independent NAV discrepancy is R$5.58794e-9. All nine ensemble/capital books incur prior-close debit on119 sessions. The619 unquoted held cells comprise611 already sourced LCAM/RENT claim cells and eight ordinary Inepar observations on December22 2022; none is terminal. LCAM carries whole entitlement before July6 delivery and only the small residual fraction afterward, through the last session before its auction terms become known. The large pre-delivery notional is not the value of that residual fraction. A bounded read of five accepted Inepar OHLC rows shows the single absent day between December21 close1.09 and December23 close1.10; it supplies no invented December22 quote, corporate event or exact-horizon endpoint. This is the existing missing-quote convention, not a new source correction.

Another204 F10 one-factor books qualify in337.58543 summed case seconds, bringing totals to594 completed and351 explicit skips. The new saved checks have maximum NAV discrepancy R$7.45058e-9. Nine recall2 books retain whole-window unresolved status, with maximum overdue principal R$49,408.77714; no new loan cash redemption occurs. All6,579 unquoted cells classify to the same LCAM claim (6,486), the Inepar gap (75) or the already frozen two-session MODL delivery hypothesis (18 on September19–20). There are no terminal unquoted cells. Existing event/loan proofs are reused, and these overlapping book observations are not independent source evidence.

The conditional denied-renewal financing rule adds18 F10 books in32.18323 summed case seconds; all54 accumulated funded variants now qualify. The new18 have no unresolved economics or loan cash redemption, with maximum NAV error R$3.72529e-9 and financing error R$4.54747e-13. Exact logs and per-book proofs remain in the primary/sensitivity/funded output directories. These totals are reconstructed from saved records; the ordinary and conditional financing contrasts remain distinct. All54 accumulated primary books and the three completed attention/GRU screen periods remain partial program results. Registration was reread after the third-period GPU group; F14 and C6 continue under the original budgets.

`ops/evaluate_matched_candidates.py` is prepared, linted and compiled but unexecuted. It replaces the earlier unexecuted replication-only helper and reuses the unchanged portfolio/sensitivity/checker drivers through one private pointer context for admitted ten-fold replication or four-fold width candidates. It also applies the qualified SOMA precision contrast to actual exposure and copies the required benchmark pointer into the private context. Intermediate qualification updates do not require source-checkout changes between steps. The replication phase list remains explicitly incomplete for newly intersecting2019/2023 corporate exposures: qualified Natura, BRML/Dommo/Copel and other event-specific hypotheses require actual-exposure disposition before final replication admission. No candidate plan, result or additional experiment is implied by preparing this helper.

All45 attention/GRU P/F fits are now complete. F14 adds18 first-pass primary books in33.06173 summed seconds; all72 accumulated primary books qualify. Maximum new NAV discrepancy is R$7.45058e-9, with no overdue principal or loan cash redemption. Ten F14 books retain the already admitted ENAT fraction with unknown auction settlement. Their largest terminal marked claim is R$21.17300 across the three capitals and seeds; at R$10m the full/wide ensembles retain R$4.46865/R$.05003, while GRU has none. These are conditional marked claims, not observed cash receipts. Large whole-share claim values before August5 delivery must not be reported as terminal fraction exposure. SOMA's16-session claim intervals remain covered by its sourced custody/auction terms.

Another201 F14 ordinary sensitivities qualify in330.17526 summed seconds, bringing totals to795 completed/465 explicit skips. Nine have overdue intervals, maximum R$3,148.22668;138 have whole-window unresolved status, mostly the separate ENAT claim. Terminal unquoted exposure is ENAT only, maximum marked R$23.46133. No loan cash redemption occurs. Eighteen conditional funded-denial variants add30.85042 summed seconds and bring that total to72; none has overdue principal, twelve retain ENAT, maximum terminal mark R$21.36043. Existing flags distinguish an overdue interval, terminal overdue principal and a locked fraction. These reconstructed counts use saved reports; no account was rerun for the narrative.

The existing35-phase list omitted the earlier qualified SOMA auction precision contrast. A separately frozen actual-claim application now reuses that exact printed50.20718 versus916532.03/18255 hypothesis, without source retrieval or repeated engineering. Six exposed wider-attention/GRU ensemble-capital variants complete once in9.98496 summed seconds;30 completed primary ensembles explicitly skip. Independent signed-fraction Decimal arithmetic and exact pre-recognition NAV/current-intention prefixes pass; initial NAV discrepancy is at most R$1.29837e-9. The R$10m direct initial cash differences are only R$.00000424/R$.00001370. Later adaptive differences retain all previous numerical uncertainty and are not a resolved economic improvement. C6 exposure remains pending; `stage_c_refit_fraction_precision_plan`, qualification and arithmetic bind this narrow addition. Current event composition, static coordinates and other hypotheses are unchanged.

C6's first slow-parent attempt failed during its compiled forward before any saved epoch/checkpoint. The six-head generator was unsupported by this compiler path. Replacing it with an explicitly ordered list preserves every tensor operation and parameter. `stage_c_refit_compile_resolution` retains the exact failed source, training driver and log. The original/new eager predictions and all gradients are exact on a synthetic933-name/full60 check using the original BF16 autocast; compiled forward/backward pass with zero score discrepancy,63.10452seconds. Dropout is zero only in this deterministic arithmetic probe. Two affected existing full-graph tests pass. The same unsupported-native-BF16 warnings remain; no precision/graph/recipe/selector/budget changes were made. Training resumes in clean isolated revision6b34109, reusing all45 completed fits; the first C6 parent subsequently completes in553.14721seconds. Remaining C6 work continues. Read-only missing filenames, output truncation, summary-key errors and a PowerShell reflog syntax error are reconstructed tooling descriptions, not numerical failures or exact standalone failed shell files.

The three completed neural-model R$10m equal-four-fold means above CDI are full attention+.9992054, wider attention−.1437482 and GRU+.6806907bps/day, under the primary marked-claim/account hypotheses. Both nominated leads therefore currently trail full attention. These saved partial-program readouts precede the C6 control and final uncertainty/disposition; no model is adopted and no ten-fold replication has been admitted. `ops/run_capacity_width.py` is prepared, linted and compiled but not frozen or run. It requires completed valid Stage C, permits exactly the registered attention96→128 and GRU64→96 cells with new P parents, and records the existing hidden-width coupling into input/peer/pooling/fusion dimensions. Final context/trunk widths stay fixed; this must not be called an isolated recurrent-matrix scaling law.

All three C6 parents subsequently completed, bringing the saved fit total to48. The first C6 fine-tuning attempt failed before a saved epoch/checkpoint: Triton's initial FileNotFoundError names a264-character temporary artifact whose parent exists, followed by a secondary compiler metadata KeyError. `stage_c_refit_cache_path_resolution` preserves the complete log and executed training/library writer. The same relative filename writes/reads at238characters under `C:/quant-data/b3/interim/ti`; only `TORCHINDUCTOR_CACHE_DIR` changes on resume. Original sources, all48 completed fits, graphs, precision, seeds, optimizer and budgets remain. The empty first-fit epochs directory is retained. This is a cache-path failure and a resumed incomplete fit, not another model/data amendment or repeated completed training.
