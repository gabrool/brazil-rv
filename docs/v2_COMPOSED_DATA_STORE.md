# Complete composed data contract

The current refit dataset combines the qualified surviving-company histories and
corporate-event outcomes in one complete store. Its purpose is to let the matched
model comparisons use consistent inputs and correct return labels. This is data
acceptance; integrated economic admission and the model comparisons remain open.

Resolve `composed_data_inputs` / `economic_refit_inputs` in the economic run pointer.
The new store is on D, with manifest
`fab704fccbd7ceed0624bcfcc67178327f392e43d124242caf8d77ff1c64dfc0`.
Both earlier accepted stores, their sources and all old fits remain immutable.
All 106 arrays, 3,717 dates, 933 permanent names, 2009 warmup and full 60-session
histories remain. Feature definitions, support thresholds and schema are unchanged.

The assembly applies the already qualified sparse wealth, daily/risk, issuer,
lending, sector, native/scalar and auxiliary changes. It does not rerun their
reducers, source censuses or the previous 185/303/124/877 consumer samples.
Sixty-seven arrays change; 39 retain their parent byte hashes. Independent complete
composition checks 2,519,791,470 changed-array cells. Nineteen combined samples
cover event boundaries, the newly eligible periods, monthly transitions and tails:
the actual dataset and collator match 224,575,552 comparisons across all names and
full histories. These are overlapping consistency checks, not independent market
observations. Three future-feature/read-capability checks pass, and the original
parent-weight transfer gate rejects the changed data before reading weights.

Final targets use the final sigma and slow risk coordinates. The physical target
builder runs once on 1,238 affected dates. Independent Decimal arithmetic verifies
596 newly usable outcomes: 275 ALSO, 300 AZZA and 21 ARZZ crossing the rename.
The 72 previously qualified NATU/ALSC/SOMA/ENAT gross outcomes and their original
endpoint proofs are reused. There are 668 total validity gains and no losses.
The unchanged raw-outcome comparison covers 34,648,044 cells. No acquired-issuer
history, missing M1 observation, source quote or post-auction ENAT unit mark is
invented. Source-entry permissions retain all liquidity membership and obligations.

The actual primary is the on-demand characteristic-neutral target. Reading it
from both complete stores on all 1,239 affected dates gives 126,777 numeric changes,
668 support gains and no losses. Its independent SVD check is part of the combined
consumer qualification. Physical rank/residual counts are separate attribution
and must not be confused with this primary or added as independent evidence.

Earlier validity dispositions remain: 620 live daily peer-field losses and 226
live issuer/lending unit-uncertainty losses versus the Natura parent, plus retired
predecessor cells. Their existing enumerations and source-specific uncertainty
barriers are reused. Earlier parent-versus-foundation losses remain separately
documented; these counts cannot simply be added across baselines. Missing SSBR/
ARZZ predecessor M1 streams remain missing. No loan alias or physical delivery
permission follows from a spot identity link.

New fits require the bound `v2_composed_store_contract.json`, fresh training-only
conditioning and new P weights; only compatible new F children inherit. Preserve
the registered architecture, folds, seeds, optimizer, selector and learning budget.
The optional to-close loss remains inactive. Accounting/source-only comparisons
continue to shallow-copy frozen OLD PolicyData. Old fits never read this store.

The target producer took 6.2806 seconds, endpoint qualification 0.7045 seconds,
successful assembly 66.0237 seconds, complete/consumer qualification 71.1859 seconds,
and actual virtual-target attribution 42.8457 seconds. These are CPU audit timings,
not model-fit estimates. The latter three numerical qualifications passed on their
first invocation. No model scoring, neural forward, GPU fit or corrected profit
measurement ran.

The initial target invocation stopped before numerical work because a hash helper
received a string instead of a Path; its exact recipe/plan/stdout are retained.
Three assembly attempts stopped on insufficient disk space; their partial staging
directories were removed by the existing atomic writer. Saved targets were reused.
After both-file SHA256 checks, 74 redundant recovery-check NPY copies were removed,
reclaiming 3,843,928,280 logical bytes. Original audit views, metadata, sparse recipes,
archives, sources and accepted stores remain. The new store uses independent files
and lossless NTFS compression; there are no hard links to a parent. Compression
alone did not resolve the space failure. The four source-entry barrier rows use
the already admitted availability dates and max(effect, knowledge) start dates.

Exact executed and failed code, logs, frozen plans, storage cleanup receipts and
new target deltas are retained. Read-only path/glob/output errors are reconstructed
tooling notes, not numerical failures or exact standalone shell files. A narrow
six-query January25 web follow-up found no additional admissible clearing evidence;
existing B3/BCB and unrelated results did not resolve the old-equity distinction.
No existing original PDF was downloaded or visually audited again.

January25 2017 still needs a dated disposition or distinct bounded cash/delivery/
rent assumptions, followed by integrated Stage A admission. ALSC credit/net cash
and ENAT auction remain explicit unknowns. Cielo precision and opposite-direction
spot-fill treatment are conditional on actual exposure. C/D remain unstarted.
The one-minute heartbeat is paused at the user's request; implementation continues
directly without routine scheduled restarts. Resolve `composed_store_recovery` for
the verified archive and full-store byte reconstruction, using existing immutable
parents and prior recoveries instead of duplicating them.
