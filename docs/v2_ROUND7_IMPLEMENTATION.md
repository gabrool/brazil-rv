# Round 7 implementation checkpoint

Round 7 is authorized and in implementation. No new experimental model score or fit has run and no GH200 has been launched for this round. The supplied registration and explicit implementation resolutions are under `research/preregistrations/`.

The source audit covers the sealed 2010–2024 development store only. Of 470 original U2 candidates, 438 lack the registered corroboration and are reclassified as large price moves without unit conversion. The other 32 are corroborated. Every event and its evidence is retained in the audit root. Existing C1 cash terms are preserved. Newly recovered prints use corrected old unit factors in their running history, so a rejected crash cannot distort subsequent inferred actions.

The parser recovered 32,530 BDI 06/07/08 quotes for 30 already established securities. Forty-seven recovered observations overlap the old active mask. These prints support valuation and exits, but cannot create new opening fills; an execution-time mask preserves earlier intended orders. The immutable original universe remains unchanged. The repaired store also reconstructs the eventual-survival audit flag, which never enters the model.

Most recovered bulletin PDF creation dates reflect later regeneration. The bounded Wayback probes supplied no earlier public capture evidence (two timed out; one returned no captures). The foreign-flow clock therefore remains unchanged. D+2 settlement is not itself proof of publication time. No forward capture is running.

The new native fundamentals branch retains signed physical values, individual receipt ages and validity masks, with training-only median/IQR scaling still to be integrated. The field-observation losses measured at the old physical-to-ranked boundary are:

| Family | Valid physical field-observations | Lost at the rank support rule |
|---|---:|---:|
| cross_market | 29,708,824 | 0 |
| events | 2,133,384 | 492 |
| fundamentals | 2,956,697 | 57,441 |
| lending | 663,384 | 9,747 |
| magnitudes | 2,260,277 | 0 |
| microstructure | 818,526 | 0 |
| options | 866,806 | 42,538 |
| rebalance | 130,544 | 0 |
| sector | 601,853 | 34,417 |

These totals sum across fields, not unique stock-days. All measured losses occurred on dates with fewer than twenty physically valid active stocks for that field. No losses on supported dates were found. Odd-lot's older source-specific builder still needs inspection. The registered native fundamentals input addresses the nine named fields; the other findings are recorded separately from the fixed factorial, rather than silently changing its input transforms.

C1 pooling, exact masked attention, no-GRU, five-head and eight-member TabM graphs are implemented. TabM expands only the shared encoded state, so history is processed once. All-family pooling has 1,471,651 parameters; attention has 1,013,283. Graph-compatible initial pretraining requires 24 fits. The 60-session lookback and all active names are retained.

Current verification: 151 targeted tests pass, including original S0 transfer behavior, C1 permutation/padding/masking, native signed values outside [-1,1], corrected action history and execution-time entry restrictions. This is implementation evidence, not evidence of predictive improvement. The repaired store, training integration, archived diagnostics and experiment runs remain pending.
