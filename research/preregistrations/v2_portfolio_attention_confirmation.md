# Rich late-attention confirmation decision

Recorded 2026-09-14 after the registered four-fold timing screen, before remaining
fits. The equal-rank ensemble has TL minus TE net CDI excess +.2530573bps/day,
utility +.2754957bps/day, and positive utility in3/4folds. Nominal paired95% utility
interval[-2.50266,4.18438] includes zero: this is a trigger, not confirmation.
The screen's ensemble accounting flags are false. All three individually paired
seed mean utility differences are positive, with intervals including zero.

The prewritten point-estimate gate is met. Complete the other10developmentfolds
for TL_all with seeds11/29/47, same compatible parent checkpoints, ASAM.2 recipe,
history, population, capacity, selection, epoch ceiling and frozen trainer.
Do not tune or refit the existing screen fits. Score the3existing TL parents for
the causal prelude using the same prelude contract as the main experiment.

Compare TL and TE on the remaining10 and all14 using the same empty-start legacy
books, head ICs and paired20-session-block intervals. Also run the registered
causal fit-only calibrated optimizer for TL on all14 with identical fit/selection/
purge, risk and accounting inputs; compare with sealed TE optimizer books using
the common10-session burn-in. Run one continuous TL optimizer book. No learned
policy, raw-objective or joint-encoder fit is added. Base4bps/proceeds1 are the
decision scenario; report2/8bps andproceeds0 without refits using the frozen
planned4bps cost. No further search or training extension follows this stage.

Call late timing supported only if paired net and utility are positive on the
remaining-fold aggregate, the screen requirement remains met and no material
accounting failure invalidates the comparison. Intervals including zero imply
exploratory evidence. Report legacy and optimizer separately: a gain under only
one controller is conditional, not a universal architecture victory. Do not use
the confirmation to promote the failed learned policy. No held-out orforward
capture access. Recover, report and stop the paid instance after this stage.
